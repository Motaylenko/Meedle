# ⚠️ ВАЖЛИВО: Перш ніж запускати проєкт

## Проблема: npm не розпізнається

Це означає, що **Node.js не встановлений** або не доданий до PATH.

---

## ✅ РІШЕННЯ: Встановіть Node.js

### Крок 1: Завантажте Node.js

1. Перейдіть на https://nodejs.org/
2. Завантажте **LTS версію** (рекомендовано)
3. Запустіть інсталятор
4. **ВАЖЛИВО:** Під час встановлення:
   - ✅ Поставте галочку "Add to PATH"
   - ✅ Поставте галочку "Install npm"

### Крок 2: Перевірте встановлення

Після встановлення **перезапустіть PowerShell** та виконайте:

```powershell
node --version
npm --version
```

Має показати версії (наприклад, v18.x.x та 9.x.x)

### Крок 3: Встановіть залежності

```powershell
cd d:\Meedle\backend
npm install

cd ..\frontend
npm install
```

### Крок 4: Запустіть проєкт

**Відкрийте 2 окремі вікна PowerShell:**

**Вікно 1:**
```powershell
cd d:\Meedle\backend
npm run dev
```

**Вікно 2:**
```powershell
cd d:\Meedle\frontend
npm run dev
```

### Крок 5: Відкрийте браузер

Перейдіть на: http://localhost:5173

---

## 🎯 Якщо Node.js вже встановлений

Але npm все одно не працює:

### Варіант 1: Перезапустіть PowerShell
Просто закрийте та відкрийте PowerShell знову.

### Варіант 2: Перевірте PATH
```powershell
$env:Path -split ';' | Select-String node
```

Має показати шлях до Node.js.

### Варіант 3: Перевстановіть Node.js
Видаліть Node.js через "Programs and Features" та встановіть знову.

---

## 📝 Покрокова інструкція (якщо нічого не працює)

### 1. Видаліть Node.js (якщо встановлений)
- Windows Settings → Apps → Node.js → Uninstall

### 2. Завантажте свіжу версію
- https://nodejs.org/
- Виберіть LTS (наприклад, 18.x.x або 20.x.x)

### 3. Встановіть з правильними налаштуваннями
```
✅ Add to PATH
✅ Install npm package manager
✅ Install tools for native modules (опціонально)
```

### 4. Перезавантажте комп'ютер
Це важливо для оновлення PATH!

### 5. Перевірте
```powershell
node --version
npm --version
```

### 6. Встановіть залежності проєкту
```powershell
cd d:\Meedle\backend
npm install

cd ..\frontend
npm install
```

### 7. Запустіть
```powershell
# Вікно 1
cd d:\Meedle\backend
npm run dev

# Вікно 2
cd d:\Meedle\frontend
npm run dev
```

---

## 🚀 Після успішного встановлення

Ви побачите:

**Backend:**
```
🚀 Meedle API Server running on port 5000
```

**Frontend:**
```
VITE ready in xxx ms
➜  Local: http://localhost:5173/
```

**Браузер:**
- Відкрийте http://localhost:5173
- Побачите Dashboard
- Все працює! 🎉

---

## ❓ Часті питання

**Q: Скільки часу займе встановлення Node.js?**
A: 2-5 хвилин

**Q: Скільки часу займе npm install?**
A: 1-3 хвилини для кожної папки

**Q: Чи потрібен інтернет?**
A: Так, для завантаження Node.js та залежностей

**Q: Яку версію Node.js вибрати?**
A: LTS версію (18.x або 20.x)

---

## 📞 Якщо все ще не працює

1. Перевірте що Node.js встановлений: `node --version`
2. Перевірте що npm працює: `npm --version`
3. Перевірте що ви в правильній папці: `pwd`
4. Перевірте що залежності встановлені: `ls node_modules`
5. Перегляньте помилки в консолі уважно

---

**Після встановлення Node.js все запрацює! 💪**
