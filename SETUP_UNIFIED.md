# Setup.bat - Об'єднана версія

## 📋 Що змінилось

Файли `setup.bat` та `setup-docker.bat` були об'єднані в один `setup.bat` з інтерактивним меню вибору.

## 🎯 Як це працює

При запуску `setup.bat` ви побачите меню:

```
========================================
   Meedle - First Time Setup
========================================

Choose setup method:

[1] Docker (Recommended)
    - No Node.js required
    - Automatic database setup
    - Isolated environment

[2] Local (Manual)
    - Requires Node.js 18+
    - Manual database setup
    - Direct on your system

========================================

Enter your choice (1 or 2):
```

## 🔧 Опція 1: Docker Setup

**Що робить:**
1. Перевіряє наявність Docker
2. Показує версію Docker
3. Завантажує необхідні образи (node:18-alpine, postgres:15-alpine)
4. Будує Docker контейнери

**Вимоги:**
- Docker Desktop встановлений та запущений

**Результат:**
```
========================================
   Docker Setup Complete!
========================================

Next steps:
1. Run start.bat to launch the application
2. Access at http://localhost:3000

Useful commands:
- start.bat : Start the application
- stop.bat  : Stop all containers
```

## 🔧 Опція 2: Local Setup

**Що робить:**
1. Перевіряє наявність Node.js
2. Показує версію Node.js
3. Встановлює залежності backend (npm install)
4. Встановлює залежності frontend (npm install)

**Вимоги:**
- Node.js 18+ встановлений

**Результат:**
```
========================================
   Local Setup Complete!
========================================

Next steps:
1. Run start.bat for Docker version
2. Or manually start:
   - Backend:  cd backend && npm run dev
   - Frontend: cd frontend && npm run dev

Note: For local setup, you'll need to run
both backend and frontend manually, or use
the old start.ps1 script.
```

## ✅ Переваги об'єднання

1. **Один файл** - простіше для користувача
2. **Вибір методу** - гнучкість для різних сценаріїв
3. **Чіткі інструкції** - зрозуміло що робити далі
4. **Перевірка вимог** - автоматична перевірка Docker/Node.js
5. **Інформативність** - показує версії встановленого ПЗ

## 🔄 Порівняння

### Раніше:
```
setup.bat          → Локальне налаштування (npm install)
setup-docker.bat   → Docker налаштування
```

### Зараз:
```
setup.bat → Меню вибору:
            [1] Docker
            [2] Local
```

## 💡 Рекомендації

- **Для більшості користувачів**: Оберіть **[1] Docker**
- **Для розробників**: Можете обрати **[2] Local** для швидшої роботи
- **Для презентацій**: Використовуйте **[1] Docker** для надійності

## 📝 Примітка

Файл `setup-docker.bat` був видалений, оскільки його функціональність тепер інтегрована в `setup.bat`.
