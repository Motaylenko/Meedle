# 🚀 Інструкція для запуску Meedle

## Варіант 1: Локальний запуск (Рекомендовано)

### Крок 1: Встановлення залежностей

#### Frontend
```powershell
cd frontend
npm install
```

#### Backend
```powershell
cd backend
npm install
```

### Крок 2: Налаштування змінних середовища

#### Frontend
Створіть файл `.env` у папці `frontend`:
```
VITE_API_URL=http://localhost:5000/api
```

#### Backend
Створіть файл `.env` у папці `backend`:
```
PORT=5000
NODE_ENV=development
```

**Примітка:** База даних не обов'язкова - backend працює з демо-даними.

### Крок 3: Запуск

Відкрийте **два окремі термінали PowerShell**:

#### Термінал 1 - Backend:
```powershell
cd backend
npm run dev
```
Backend запуститься на `http://localhost:5000`

#### Термінал 2 - Frontend:
```powershell
cd frontend
npm run dev
```
Frontend запуститься на `http://localhost:5173`

### Крок 4: Відкрийте у браузері

Перейдіть за адресою: **http://localhost:5173**

---

## Варіант 2: Запуск з Docker

### Передумови
- Встановлений Docker Desktop

### Команди

```powershell
# У кореневій папці проєкту
docker compose up --build
```

**Примітка:** У новіших версіях Docker використовується `docker compose` (без дефіса).

Після запуску:
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000
- PostgreSQL: localhost:5432

---

## 🎯 Функціонал

### Реалізовано:

✅ **Персоналізація**
- Темна та світла теми
- Збереження налаштувань у localStorage

✅ **Система рейтингу**
- Відображення поточного рейтингу
- Таблиця лідерів
- Позиція студента

✅ **Інтерактивний розклад**
- Тижневий розклад занять
- Відображення найближчих пар
- Інформація про викладачів та аудиторії

✅ **Кабінет студента**
- Dashboard з статистикою
- Активні завдання
- Оцінки по курсам
- Налаштування профілю

✅ **API Backend**
- REST API для всіх даних
- Демо-дані (працює без БД)
- CORS налаштування

✅ **Push-сповіщення**
- Сервіс для браузерних сповіщень
- Різні типи сповіщень (завдання, розклад, оцінки)

---

## 📁 Структура проєкту

```
Meedle/
├── frontend/                 # React + Vite
│   ├── src/
│   │   ├── components/      # Компоненти (Header)
│   │   ├── pages/           # Сторінки (Dashboard, Schedule, etc.)
│   │   ├── services/        # API та сервіси
│   │   └── styles/          # Загальні стилі
│   ├── package.json
│   └── vite.config.js
│
├── backend/                  # Node.js + Express
│   ├── src/
│   │   └── index.js         # Головний файл API
│   ├── package.json
│   └── Dockerfile
│
├── docker-compose.yml        # Docker конфігурація
└── README.md                 # Документація проєкту
```

---

## 🔧 Налагодження

### Frontend не запускається

1. Видаліть `node_modules` та `package-lock.json`
2. Виконайте `npm install` знову

### Backend не запускається

1. Перевірте, чи вільний порт 5000
2. Перевірте файл `.env`
3. Backend працює БЕЗ бази даних (використовує демо-дані)

### Порт вже зайнятий

Змініть порт у відповідних файлах:
- Frontend: `vite.config.js` → `server.port`
- Backend: `.env` → `PORT`

---

## 📝 API Endpoints

### Courses
- `GET /api/courses` - Список курсів
- `GET /api/courses/:id` - Деталі курсу

### Schedule
- `GET /api/schedule` - Повний розклад
- `GET /api/schedule/today` - Розклад на сьогодні

### Grades
- `GET /api/grades` - Оцінки студента

### Leaderboard
- `GET /api/leaderboard` - Таблиця лідерів
- `GET /api/leaderboard/top/:count` - Топ N студентів

### Tasks
- `GET /api/tasks` - Всі завдання
- `GET /api/tasks/active` - Активні завдання
- `POST /api/tasks/:id/status` - Оновити статус

### User
- `GET /api/user` - Дані користувача
- `PUT /api/user/settings` - Оновити налаштування

### Dashboard
- `GET /api/dashboard/stats` - Статистика для dashboard

---

## 🎨 Особливості дизайну

- **Сучасний UI** з градієнтами та анімаціями
- **Темна/Світла теми** з плавним перемиканням
- **Адаптивний дизайн** для мобільних пристроїв
- **Гейміфікація** з рейтингами та досягненнями
- **Інтерактивні елементи** з hover-ефектами

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте, чи встановлені всі залежності
2. Переконайтеся, що порти 5000 та 5173 вільні
3. Перегляньте консоль браузера на наявність помилок
