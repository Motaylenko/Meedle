# 🔧 Вирішення проблем з запуском

## Проблема: Сайт не відкривається після start.bat

### Рішення 1: Використайте PowerShell скрипт (РЕКОМЕНДОВАНО)

1. **Клікніть правою кнопкою** на `start.ps1`
2. Виберіть **"Run with PowerShell"**
3. Якщо з'явиться помилка про політику виконання, виконайте:
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```
4. Запустіть `start.ps1` знову

---

### Рішення 2: Ручний запуск (НАЙНАДІЙНІШЕ)

#### Крок 1: Встановіть залежності

**Відкрийте PowerShell** і виконайте:

```powershell
# Перейдіть до папки проєкту
cd d:\Meedle

# Встановіть залежності для backend
cd backend
npm install
cd ..

# Встановіть залежності для frontend
cd frontend
npm install
cd ..
```

#### Крок 2: Запустіть сервери

**Відкрийте ДВА окремі вікна PowerShell:**

**Вікно 1 - Backend:**
```powershell
cd d:\Meedle\backend
npm run dev
```

Чекайте поки побачите:
```
🚀 Meedle API Server running on port 5000
```

**Вікно 2 - Frontend:**
```powershell
cd d:\Meedle\frontend
npm run dev
```

Чекайте поки побачите:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
```

#### Крок 3: Відкрийте браузер

Перейдіть на: **http://localhost:5173**

---

### Рішення 3: Перевірка помилок

Якщо сервери не запускаються, перевірте:

#### 1. Node.js встановлений?
```powershell
node --version
npm --version
```

Якщо помилка - встановіть Node.js з https://nodejs.org/

#### 2. Порти вільні?
```powershell
# Перевірка порту 5000 (backend)
netstat -ano | findstr :5000

# Перевірка порту 5173 (frontend)
netstat -ano | findstr :5173
```

Якщо порти зайняті, зупиніть процеси або змініть порти.

#### 3. Залежності встановлені?
```powershell
# Перевірте чи існують папки
Test-Path d:\Meedle\backend\node_modules
Test-Path d:\Meedle\frontend\node_modules
```

Має повернути `True` для обох.

---

### Рішення 4: Повне очищення та перевстановлення

Якщо нічого не допомагає:

```powershell
cd d:\Meedle

# Видаліть node_modules
Remove-Item -Recurse -Force backend\node_modules -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend\node_modules -ErrorAction SilentlyContinue

# Видаліть package-lock.json
Remove-Item backend\package-lock.json -ErrorAction SilentlyContinue
Remove-Item frontend\package-lock.json -ErrorAction SilentlyContinue

# Встановіть знову
cd backend
npm install
cd ..\frontend
npm install
cd ..
```

Потім запустіть вручну (Рішення 2).

---

## Типові помилки та рішення

### Помилка: "npm is not recognized"
**Рішення:** Встановіть Node.js з https://nodejs.org/

### Помилка: "EADDRINUSE: address already in use"
**Рішення:** Порт зайнятий. Зупиніть інший процес або змініть порт:
```powershell
# Знайдіть процес
netstat -ano | findstr :5000

# Зупиніть процес (замініть PID)
taskkill /PID <PID> /F
```

### Помилка: "Cannot find module"
**Рішення:** Перевстановіть залежності:
```powershell
cd backend
Remove-Item -Recurse node_modules
npm install
```

### Помилка: "CORS error" в браузері
**Рішення:** Переконайтеся що backend запущений на порту 5000

---

## Швидка перевірка

Виконайте ці команди для діагностики:

```powershell
# 1. Версія Node.js
node --version

# 2. Версія npm
npm --version

# 3. Чи існують залежності
Test-Path backend\node_modules
Test-Path frontend\node_modules

# 4. Чи вільні порти
netstat -ano | findstr :5000
netstat -ano | findstr :5173
```

---

## Контакт для підтримки

Якщо проблема не вирішується:

1. Перевірте всі кроки вище
2. Перегляньте помилки в консолі
3. Перевірте чи Node.js версії 18+
4. Спробуйте перезавантажити комп'ютер

---

## Успішний запуск виглядає так:

**Backend консоль:**
```
🚀 Meedle API Server running on port 5000
📍 http://localhost:5000
💾 Database: Mock Data
```

**Frontend консоль:**
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**Браузер:**
- Відкривається http://localhost:5173
- Бачите Dashboard з даними
- Навігація працює
- Теми перемикаються

---

**Якщо все працює - вітаємо! 🎉**
