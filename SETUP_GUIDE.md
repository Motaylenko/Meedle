# 🚀 Оновлений Setup Script

## ✅ Що змінилося:

**Об'єднано `setup.bat` і `fix-docker.bat` в один покращений файл!**

Тепер `setup.bat` має **4 режими роботи**:

---

## 📋 Режими Setup

### **[1] Fresh Install** (Рекомендовано для першого разу)
- ✅ Повне очищення старих контейнерів
- ✅ Завантаження Docker образів
- ✅ Збірка контейнерів
- ✅ Налаштування Prisma
- ✅ Заповнення БД даними

**Коли використовувати:**
- Перший раз встановлюєте проєкт
- Хочете почати з чистого аркуша
- Потрібна повна ініціалізація БД

---

### **[2] Quick Setup** (Швидке налаштування)
- ✅ Збірка контейнерів
- ✅ Запуск сервісів
- ⚠️ Зберігає існуючі дані

**Коли використовувати:**
- Вже встановлювали раніше
- Потрібно швидко перезібрати
- Хочете зберегти дані в БД

---

### **[3] Fix/Rebuild** (Виправлення проблем)
- ✅ Повне очищення (включно з кешем)
- ✅ Збірка без кешу
- ✅ Повне налаштування Prisma
- ✅ Діагностика проблем

**Коли використовувати:**
- Виникають помилки при збірці
- Docker контейнери не працюють
- Потрібно виправити проблеми з Prisma
- Після оновлення залежностей

---

### **[4] Local Setup** (Без Docker)
- ✅ Встановлення залежностей Node.js
- ✅ Генерація Prisma Client
- ⚠️ Потрібна ручна БД

**Коли використовувати:**
- Не хочете використовувати Docker
- Маєте власний PostgreSQL
- Розробка без контейнерів

---

## 🎯 Використання:

### Запуск:
```bash
setup.bat
```

### Виберіть режим:
```
Choose setup option:

[1] Fresh Install (Recommended for first time)
[2] Quick Setup (If already installed before)
[3] Fix/Rebuild (If having issues)
[4] Local Setup (Without Docker)

Enter your choice (1-4):
```

---

## 🔧 Що робить кожен режим:

### Fresh Install (Режим 1):
```
[1/6] Cleaning old containers...
[2/6] Pulling required Docker images...
[3/6] Building Docker containers...
[4/6] Starting database...
[5/6] Setting up Prisma...
[6/6] Seeding database with initial data...
```

### Quick Setup (Режим 2):
```
[1/2] Building containers...
[2/2] Starting services...
```

### Fix/Rebuild (Режим 3):
```
[1/7] Stopping all containers...
[2/7] Cleaning Docker cache...
[3/7] Building containers (no cache)...
[4/7] Starting database...
[5/7] Setting up Prisma...
[6/7] Seeding database...
[7/7] Starting all services...
```

### Local Setup (Режим 4):
```
[1/4] Installing Backend Dependencies...
[2/4] Setting up Prisma...
[3/4] Installing Frontend Dependencies...
[4/4] Database setup instructions...
```

---

## ✨ Нові можливості:

### 1. **Автоматична діагностика**
- Перевірка статусу контейнерів
- Тестування API підключення
- Відображення корисних команд

### 2. **Покращена обробка помилок**
- Альтернативні методи при помилках
- Детальні повідомлення про проблеми
- Інструкції по виправленню

### 3. **Візуальні індикатори**
- ✅ Успішні операції
- ⚠️ Попередження
- 🌐 Посилання на сервіси
- 📊 Корисні команди

---

## 📊 Після завершення:

```
========================================
   Access Your Application:
========================================

🌐 Frontend:      http://localhost:3000
🔧 Backend API:   http://localhost:5000
💾 Database:      localhost:5432
📊 Prisma Studio: docker compose exec backend npm run db:studio

========================================
   Useful Commands:
========================================

▶️  Start:        start.bat
⏹️  Stop:         stop.bat
📋 View logs:     docker compose logs -f
🔄 Restart:       docker compose restart
🗑️  Clean all:     docker compose down -v
```

---

## 🆘 Вирішення проблем:

### Якщо виникла помилка при Fresh Install:
→ Спробуйте **[3] Fix/Rebuild**

### Якщо API не відповідає:
```bash
docker compose logs backend
```

### Якщо БД не підключається:
```bash
docker compose restart db
timeout /t 10
docker compose exec backend npm run db:push
```

### Якщо Prisma Client не знайдено:
```bash
docker compose exec backend npm run db:generate
```

---

## 🎉 Готово!

Тепер у вас є **один універсальний скрипт** для всіх сценаріїв налаштування!

**Рекомендація:**
- Перший раз: **[1] Fresh Install**
- Повторно: **[2] Quick Setup**
- Проблеми: **[3] Fix/Rebuild**
- Без Docker: **[4] Local Setup**
