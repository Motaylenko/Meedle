# Meedle - Міграція на Docker

## 📋 Зміни в проєкті

### ✅ Що було зроблено

1. **Оновлено `start.bat`**
   - Тепер запускає додаток через Docker Compose
   - Автоматична перевірка наявності Docker
   - Зупинка старих контейнерів перед запуском
   - Автоматичне відкриття браузера на http://localhost:3000
   - Інформативні повідомлення про статус

2. **Створено нові файли:**
   - `stop.bat` - зупинка всіх Docker контейнерів
   - `backend/.dockerignore` - оптимізація збірки backend
   - `frontend/.dockerignore` - оптимізація збірки frontend
   - `DOCKER_GUIDE.md` - повна документація по Docker (українською)
   - `DOCKER_QUICK_REF.md` - швидкий довідник команд

3. **Оновлено існуючі файли:**
   - `setup.bat` - тепер підтримує вибір між Docker та локальним налаштуванням
   - `README.md` - Docker тепер рекомендований метод запуску
   - Додано посилання на нові гайди

### 📁 Нові та оновлені файли

```
Meedle/
├── start.bat                  ✨ ОНОВЛЕНО - Docker версія
├── setup.bat                  ✨ ОНОВЛЕНО - вибір Docker/Local
├── stop.bat                   ✨ НОВИЙ - зупинка контейнерів
├── DOCKER_GUIDE.md            ✨ НОВИЙ - повний гайд
├── DOCKER_QUICK_REF.md        ✨ НОВИЙ - швидкий довідник
├── README.md                  ✨ ОНОВЛЕНО - Docker як основний метод
├── backend/
│   └── .dockerignore          ✨ НОВИЙ - оптимізація
└── frontend/
    └── .dockerignore          ✨ НОВИЙ - оптимізація
```

### 🔄 Що залишилось без змін

- `setup.bat` - все ще працює для локального запуску
- `start.ps1` - PowerShell версія для локального запуску
- `docker-compose.yml` - вже був налаштований
- `backend/Dockerfile` - вже був налаштований
- `frontend/Dockerfile` - вже був налаштований
- Весь код frontend та backend

## 🚀 Як користуватись

### Перший запуск

```batch
# 1. Встановіть Docker Desktop (якщо ще не встановлено)
# Завантажте з: https://www.docker.com/products/docker-desktop

# 2. Запустіть Docker Desktop

# 3. Виконайте setup
setup.bat
# Оберіть [1] Docker (Рекомендовано)

# 4. Запустіть додаток
start.bat
```

### Щоденне використання

```batch
# Запуск
start.bat

# Зупинка
stop.bat
```

### Корисні команди

```batch
# Логи всіх сервісів
docker compose logs -f

# Логи тільки frontend
docker compose logs -f frontend

# Перезапуск
docker compose restart

# Повна перебудова
docker compose up -d --build

# Видалити все (включно з даними БД)
docker compose down -v
```

## 🌐 URL адреси

| Сервіс | URL | Опис |
|--------|-----|------|
| Frontend | http://localhost:3000 | React додаток |
| Backend | http://localhost:5000 | API сервер |
| Database | localhost:5432 | PostgreSQL |

**Зверніть увагу:** Frontend тепер на порту **3000** (а не 5173 як раніше)

## 📊 Порівняння методів запуску

| Аспект | Docker (start.bat) | Локальний (setup.bat) |
|--------|-------------------|----------------------|
| Node.js | Не потрібен ✅ | Потрібен ❌ |
| PostgreSQL | Автоматично ✅ | Вручну ❌ |
| Налаштування | 1 команда ✅ | Багато кроків ❌ |
| Ізоляція | Повна ✅ | Немає ❌ |
| Портативність | Висока ✅ | Низька ❌ |
| Швидкість | Трохи повільніше | Швидше ✅ |

## 🔧 Troubleshooting

### Проблема: "Docker is not installed"

**Рішення:**
1. Встановіть Docker Desktop
2. Запустіть Docker Desktop
3. Почекайте поки Docker повністю завантажиться
4. Спробуйте знову

### Проблема: Порт вже зайнятий

**Рішення:**
```batch
# Зупиніть існуючі контейнери
docker compose down

# Або змініть порти в docker-compose.yml
```

### Проблема: Зміни в коді не відображаються

**Рішення:**
```batch
# Перезапустіть контейнер
docker compose restart frontend

# Або повна перебудова
docker compose up -d --build
```

## 📚 Додаткові ресурси

- [DOCKER_GUIDE.md](./DOCKER_GUIDE.md) - Повна документація
- [DOCKER_QUICK_REF.md](./DOCKER_QUICK_REF.md) - Швидкий довідник
- [README.md](./README.md) - Загальна інформація про проєкт
- [STARTUP_GUIDE.md](./STARTUP_GUIDE.md) - Детальна інструкція запуску

## ✨ Переваги нової версії

1. **Простота** - один клік для запуску
2. **Надійність** - ізольоване середовище
3. **Портативність** - працює однаково скрізь
4. **Автоматизація** - база даних налаштовується автоматично
5. **Професійність** - використання індустріальних стандартів

---

**Примітка:** Старі методи запуску (`setup.bat`, `start.ps1`) все ще працюють для тих, хто хоче запускати без Docker.
