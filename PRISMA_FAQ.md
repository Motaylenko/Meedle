# ❓ Prisma FAQ - Часті питання

## 🤔 Загальні питання:

### 1. Що таке Prisma?
Prisma - це сучасний ORM (Object-Relational Mapping) для Node.js та TypeScript. Він дозволяє працювати з базою даних через зручний API замість написання SQL запитів.

### 2. Чому Prisma, а не інші ORM?
- ✅ Автоматична генерація типів (TypeScript)
- ✅ Зручний Prisma Studio (GUI для БД)
- ✅ Міграції з коробки
- ✅ Відмінна документація
- ✅ Швидкість та безпека

### 3. Чи потрібно знати SQL для роботи з Prisma?
Ні! Prisma генерує SQL автоматично. Але знання SQL допоможе краще розуміти, що відбувається під капотом.

---

## 🛠️ Налаштування:

### 4. Як змінити підключення до БД?
Відредагуйте `backend/.env`:
```bash
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
```

### 5. Як працювати з Docker?
```bash
# Використовуйте ім'я сервісу замість localhost
DATABASE_URL="postgresql://meedle_user:meedle_password@db:5432/meedle_db"
```

### 6. Як змінити схему БД?
1. Відредагуйте `backend/prisma/schema.prisma`
2. Виконайте `npm run db:generate`
3. Виконайте `npm run db:push`

---

## 🔧 Робота з даними:

### 7. Як додати нову таблицю?
Додайте модель у `schema.prisma`:
```prisma
model NewTable {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
  
  @@map("new_table")
}
```
Потім: `npm run db:generate && npm run db:push`

### 8. Як додати нове поле до існуючої таблиці?
Додайте поле у модель:
```prisma
model User {
  id    Int    @id
  name  String
  phone String? // нове поле (опціональне)
}
```
Потім: `npm run db:generate && npm run db:push`

### 9. Як видалити таблицю?
Видаліть модель з `schema.prisma` та виконайте `npm run db:push`

---

## 📊 Запити:

### 10. Як отримати всі записи?
```javascript
const users = await prisma.user.findMany();
```

### 11. Як отримати один запис?
```javascript
const user = await prisma.user.findUnique({
  where: { id: 1 }
});
```

### 12. Як створити новий запис?
```javascript
const user = await prisma.user.create({
  data: {
    name: 'Іван',
    email: 'ivan@example.com'
  }
});
```

### 13. Як оновити запис?
```javascript
const user = await prisma.user.update({
  where: { id: 1 },
  data: { name: 'Новий Іван' }
});
```

### 14. Як видалити запис?
```javascript
await prisma.user.delete({
  where: { id: 1 }
});
```

### 15. Як отримати дані з зв'язками?
```javascript
const user = await prisma.user.findUnique({
  where: { id: 1 },
  include: {
    settings: true,
    enrollments: {
      include: {
        course: true
      }
    }
  }
});
```

---

## 🐛 Вирішення проблем:

### 16. Помилка: "Can't reach database server"
**Причина:** PostgreSQL не запущений або неправильний DATABASE_URL

**Рішення:**
```bash
# Перевірте, чи запущений Docker
docker-compose ps

# Запустіть БД
docker-compose up db -d

# Перевірте .env файл
```

### 17. Помилка: "Prisma Client not found"
**Причина:** Prisma Client не згенерований

**Рішення:**
```bash
npm run db:generate
```

### 18. Помилка: "Table does not exist"
**Причина:** Таблиці не створені в БД

**Рішення:**
```bash
npm run db:push
npm run db:seed
```

### 19. Помилка: "Port 5000 already in use"
**Причина:** Інший процес використовує порт 5000

**Рішення:**
```bash
# Змініть PORT в .env
PORT=5001

# Або зупиніть інший процес
```

### 20. Помилка: "Migration failed"
**Причина:** Конфлікт міграцій або схеми

**Рішення:**
```bash
# Скиньте БД (ОБЕРЕЖНО!)
npx prisma migrate reset

# Або використовуйте db:push
npm run db:push
```

---

## 🔄 Міграції:

### 21. Коли використовувати `db:push`?
- ✅ Під час розробки
- ✅ Для швидкого прототипування
- ✅ Коли не потрібна історія змін

### 22. Коли використовувати `db:migrate`?
- ✅ Для production
- ✅ Коли потрібна історія змін
- ✅ Для командної роботи

### 23. Як відкотити міграцію?
```bash
# Скинути всі міграції
npx prisma migrate reset

# Або вручну видалити файл міграції
```

---

## 🎯 Оптимізація:

### 24. Як прискорити запити?
- Використовуйте `select` замість `include` коли можливо
- Додайте індекси в schema.prisma
- Використовуйте пагінацію

### 25. Як додати індекс?
```prisma
model User {
  id    Int    @id
  email String @unique // автоматичний індекс
  name  String
  
  @@index([name]) // додатковий індекс
}
```

### 26. Як зробити пагінацію?
```javascript
const users = await prisma.user.findMany({
  skip: 0,
  take: 10, // перші 10 записів
});
```

---

## 🔐 Безпека:

### 27. Чи безпечний Prisma від SQL ін'єкцій?
Так! Prisma автоматично екранує всі параметри.

### 28. Як захистити .env файл?
- ✅ Додайте `.env` до `.gitignore` (вже додано)
- ✅ Ніколи не комітьте `.env` в Git
- ✅ Використовуйте `.env.example` для документації

---

## 🚀 Production:

### 29. Як задеплоїти з Prisma?
```bash
# 1. Встановіть залежності
npm install

# 2. Згенеруйте Client
npm run db:generate

# 3. Застосуйте міграції
npm run db:migrate

# 4. Запустіть сервер
npm start
```

### 30. Як налаштувати для production?
```bash
# .env для production
NODE_ENV=production
DATABASE_URL="postgresql://user:pass@production-host:5432/db"
```

---

## 📚 Додаткові ресурси:

### 31. Де знайти більше інформації?
- 📖 [Офіційна документація Prisma](https://www.prisma.io/docs)
- 🎓 [Prisma Tutorial](https://www.prisma.io/docs/getting-started)
- 💬 [Prisma Discord](https://pris.ly/discord)
- 🐙 [Prisma GitHub](https://github.com/prisma/prisma)

### 32. Які є альтернативи Prisma?
- Sequelize
- TypeORM
- Knex.js
- Mongoose (для MongoDB)

---

## 🎓 Навчання:

### 33. Як навчитися Prisma?
1. Прочитайте `PRISMA_QUICKSTART.md`
2. Виконайте всі кроки з `PRISMA_CHECKLIST.md`
3. Експериментуйте з Prisma Studio
4. Читайте офіційну документацію
5. Практикуйтесь на реальних проєктах

### 34. Які є корисні команди для дебагу?
```bash
# Переглянути згенерований SQL
npx prisma studio

# Переглянути статус міграцій
npx prisma migrate status

# Валідувати schema
npx prisma validate

# Форматувати schema
npx prisma format
```

---

## 💡 Поради:

### 35. Найкращі практики:
- ✅ Завжди використовуйте `db:generate` після зміни schema
- ✅ Використовуйте міграції для production
- ✅ Додавайте індекси для часто використовуваних полів
- ✅ Використовуйте транзакції для критичних операцій
- ✅ Логуйте всі запити під час розробки

### 36. Чого НЕ робити:
- ❌ Не комітьте `.env` в Git
- ❌ Не використовуйте `db:push` на production
- ❌ Не забувайте про індекси
- ❌ Не робіть N+1 запити (використовуйте `include`)
- ❌ Не забувайте про `disconnect()` при завершенні

---

## 🎉 Готово!

Якщо у вас є інші питання, перевірте документацію або напишіть мені! 🚀
